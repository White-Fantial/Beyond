/**
 * Kitchen display layout — intentionally bare, no sidebar or header.
 * Designed for a dedicated full-screen tablet mounted in the kitchen.
 */
export default function KitchenLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
